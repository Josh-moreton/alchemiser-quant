# Environment Variables for Almgren-Chriss Execution

# Almgren-Chriss Optimal Execution Model Parameters
# See docs/ALMGREN_CHRISS_EXECUTION.md for detailed documentation

# Risk aversion parameter (λ): Higher values = more aggressive execution
# Range: 0.1 (patient) to 2.0 (aggressive)
# Default: 0.5 (balanced)
AC_RISK_AVERSION=0.5

# Volatility (σ): Expected volatility per sqrt(dt)
# Range: 0.01 (low volatility) to 0.05 (high volatility)
# Default: 0.02 (medium volatility)
AC_VOLATILITY=0.02

# Temporary market impact (η): Immediate price impact coefficient
# Range: 0.0001 (liquid) to 0.01 (illiquid)
# Default: 0.001 (mid cap liquidity)
AC_TEMP_IMPACT=0.001

# Number of time slices (N): How many slices to split the order into
# Range: 3 (small orders) to 10 (large orders)
# Default: 5
AC_NUM_SLICES=5

# Total execution time (T): Total time horizon in seconds
# Range: 60 (quick) to 600 (patient)
# Default: 300 (5 minutes)
AC_TOTAL_TIME_SECONDS=300

# Wait time per slice: How long to wait for each slice to fill
# Range: 10 (aggressive) to 60 (patient)
# Default: 30 seconds
AC_SLICE_WAIT_SECONDS=30

# Market order fallback: Use market order if execution stalls (<50% filled)
# Values: true or false
# Default: true
AC_MARKET_FALLBACK=true
