from setuptools import setup, find_packages

setup(
    name="the_alchemiser",
    version="0.1.0",
    packages=find_packages(include=["the_alchemiser", "the_alchemiser.*"]),
    install_requires=[],
)
